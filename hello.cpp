#include <bits/stdc++.h>
using namespace std;

// f**k

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);

  // vector<int> nums = {-2, 1, -3, 4, -1, 2, 1, -5, 4};
  // vector<vector<int>> nums(n, vector<int>(m, 0));
  // string str1 = "abcde";
  // string str2 = "ace";

  return 0;
}