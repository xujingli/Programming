//日期：2018/4/22 时间：
/*
1.C语言中的qsort函数，C++中的sort函数。qsort涉及到很多指针的使用，
	并且sort函数在实现中，规避了经典快速排序中可能出现的会导致实际复杂度为O(n2)的极端情况。因此更推荐后者。 
	#include <algorithm> using namespace std;
	sort(首元素地址(必填)，尾元素地址的下一个地址(必填),比较函数（非必填）); //默认递增排序 
	
2.读题要迅速，快速提炼里面的本质。不能在读题上浪费时间，因此训练的要点是：决定做题，就把自己全部关闭起来。
 
*/ 
